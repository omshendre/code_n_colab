/// we are doing this because we are using it alot of times so typos in our code could be problematic so er are storing them and using them when necessary
const ACTIONS = {
    JOIN: "join",
    JOINED: "joined",
    DISCONNECTED: "disconnected",
    CODE_CHANGE: "code-change",
    SYNC_CODE: "sync-code",
    LEAVE: "leave",
  };
  
  module.exports= {ACTIONS}
  